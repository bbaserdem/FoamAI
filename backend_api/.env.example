# -----------------------------------------------------------------------------
# Example Environment Variables for FoamAI Backend
# -----------------------------------------------------------------------------
#
# Copy this file to a new file named .env in the same directory (`backend_api/`)
# and replace the placeholder values with your actual configuration.
# The .env file should NOT be committed to version control.
#
# --- General Application Settings ---
# The host/IP address of your EC2 instance or server where the API is running.
EC2_HOST=your_ec2_ip_address_here

# The base directory on the server where project/case files are stored.
FOAM_RUN=/path/to/your/foam/run/directory
